- User docs follow-up: the **Installation → "External dependencies"** section (on the `docs` branch) still says `ffmpeg` + `go2rtc` are Windows-only auto-download. That's now outdated — both auto-download on **Linux too** (x86_64 + aarch64): go2rtc as a raw binary, ffmpeg as a static `.tar.xz` unpacked via the system `tar`; unsupported arch / failure → a manual-install hint. Update that section when next on the docs branch.
