- **BUG — Linux video-helper auto-download fails.** The ffmpeg + go2rtc Linux auto-download (1dcb848) does **not** work on at least one test machine — investigate before relying on it. Until fixed, do **not** document Linux auto-download as working (the Installation → "External dependencies" user-docs section should keep treating Linux as manual-install / TBD). Revisit the download logic in `src-tauri/src/video/{ffmpeg,go2rtc}.rs` (arch detection, asset name, tar extraction, exec perms) + capture the failing machine's arch + error.
